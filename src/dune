(include_subdirs unqualified)

(library 
  (name fly_lib) 
  (modules semant sast ast parser scanner prints utils))

(executable 
  (name fly) 
  (public_name fly) 
  (modules fly) 
  (libraries fly_lib))

(ocamllex scanner)
(ocamlyacc parser)
